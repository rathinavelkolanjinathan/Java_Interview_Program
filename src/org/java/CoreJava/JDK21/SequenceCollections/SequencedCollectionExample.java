package org.java.CoreJava.JDK21.SequenceCollections;

import java.util.ArrayList;
//import java.util.SequencedCollection;

//Sequenced Collections are a feature introduced in Java 21 to provide a unified interface
// for working with collections. This new feature provides simple methods to fetch the
// first and the last elements of a collection. The three new interfaces are:
//
//SequencedCollection
//SequencedSet
//SequencedMap
public class SequencedCollectionExample {
  /*  public static void main(String[] args) {

        SequencedCollection<String> names = new ArrayList<>();

        // Adding elements
        names.addFirst("Alice");
        names.addLast("Bob");
        names.addFirst("Charlie");
        names.addLast("Robert");

        // Accessing elements
        System.out.println("First: " + names.getFirst());
        System.out.println("Last: " + names.getLast());

        // Removing elements
        names.removeFirst();
        System.out.println("After removeFirst: " + names);

        names.removeLast();
        System.out.println("After removeLast: " + names);
    }*/
}