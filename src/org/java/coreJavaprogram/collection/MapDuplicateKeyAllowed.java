package org.collection;
import java.util.List;


public class MapDuplicateKeyAllowed {
public static void main(String[] args) {
	List list;
    /*org.apache.commons.collections4.map.MultiValueMap map = new org.apache.commons.collections4.map.MultiValueMap();
    map.put("A", 4);
    map.put("A", 6);
    map.put("B", 7);
    map.put("C", 1);
    map.put("B", 9);
    map.put("A", 5);

    Set entrySet = map.entrySet();
    Iterator it = entrySet.iterator();
    System.out.println("  Object key  Object value");
    while (it.hasNext()) {
        Map.Entry mapEntry = (Map.Entry) it.next();
        list = (List) map.get(mapEntry.getKey());
        for (int j = 0; j < list.size(); j++) {
            System.out.println("\t" + mapEntry.getKey() + "\t  " + list.get(j));
        }
    }*/
}
}

